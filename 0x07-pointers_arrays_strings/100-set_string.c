#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * set_string - sets the value of a pointer to a char
 * @s: double pointer
 * @to: string
 * Return: Nothing
 */

void set_string(char **s, char *to)
{
*s = to;
}
