#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * _atoi - convert a string to an integer
 * @s: string to convert
 * Return: Integer number
 */
int _atoi(char *s)
{
int tmp;
tmp = atoi(s);
return (tmp);
}
